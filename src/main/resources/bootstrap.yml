spring:
  profiles: example_profile
  cloud:
      config:
        uri: http://plt-stg-cfg.ecs2.uken.int

spring:
  profiles: platform_staging
  cloud:
      config:
        uri: http://plt-stg-cfg.ecs2.uken.int
